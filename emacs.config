;; -*- mode: emacs-lisp -*-
;; this store emacs config and will get load at the end of configuration
;; It must be stored in dotfiles directory.

(setq mac-option-modifier 'meta)
(setq mac-command-modifier 'super)

(global-visual-line-mode 1)
(server-start)

